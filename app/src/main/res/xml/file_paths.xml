<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- share anything you create in the cache directory -->
    <cache-path
        name="cache"
        path="." />

    <!-- share anything you store in files/ (keeps existing behaviour) -->
    <external-files-path
        name="external_files"
        path="." />

</paths>
